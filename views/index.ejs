<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>All Post</title>
    <link rel="stylesheet" href="/style.css">
    
  </head>
  <body>
    
    <h1>Foodies  Recipes </h1> 
    <% for(post of posts){%>
        <div class = "post">
            <h3 class = "user">@<%= post.username %></h3>
            <% if(post.image) { %>
              <img src="<%= post.image %>" alt="Post Image" style="max-width: 300px;">
            <% } %>
            <h4 class = "content"><%= post.content %></h4>
            <br>
            <a href="http://localhost:7000/posts/<%=post.id%>">see  in details</a>

            <a href="http://localhost:7000/posts/<%=post.id%>/edit">Edit</a>
            <form method="post" action="/posts/<%= post.id%>?_method=DELETE">
              <button>Delect Post</button>
            </form>
            

        </div>
    <%}%>
    
    <br />
    <br />

    <a href="http://localhost:7000/posts/new"> Create new post</a>
  </body>
</html>